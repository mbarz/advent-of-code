import { solvePart1, solvePart2 } from './2413';

describe('2024 Day 13', () => {
  it('should solve part 1', () => {
    expect(solvePart1('')).toEqual(0);
  });

  it('should solve part 1', () => {
    expect(solvePart2('')).toEqual(0);
  });
});
